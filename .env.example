# PostgreSQL superuser (used by the postgres container itself)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

# Kong database user password (created by initdb script)
KONG_PG_PASSWORD=changeme

# Application service database passwords (created by initdb script)
APP_RATESAGEAI_PASSWORD=changeme
